# ------------------------------
# Python / Django
# ------------------------------

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
.venv/
ENV/
env/
env.bak/
venv.bak/

# Django stuff
*.log
*.pot
*.mo
*.sqlite3
db.sqlite3             # SQLite files should not be tracked
media/                 # Uploaded media files
staticfiles/           # collectstatic output
local_settings.py      # local overrides
__pycache__/
*.py[cod]

# Django migrations (optional: commit shared migrations, ignore tenant-specific)
# Keep initial migrations for your apps (users/core/common)
# If you want to ignore all generated migrations uncomment below:
# */migrations/*.py
# !*/migrations/__init__.py

# ------------------------------
# Environment / secrets
# ------------------------------
*.env
*.key
*.pem

# ------------------------------
# Testing / coverage
# ------------------------------
.pytest_cache/
.tox/
.coverage
coverage.xml
*.cover
*.py,cover
htmlcov/

# ------------------------------
# Editors / IDEs
# ------------------------------
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# ------------------------------
# Docker
# ------------------------------
docker-compose.override.yml
*.pid
*.sock
*.log
# Ignore container volumes (these hold DB files)
docker/postgres-data/
docker/pgadmin-data/

# ------------------------------
# RAG evaluation harness (backend/evaluation)
# ------------------------------
# Generated run outputs — never commit. report.md/json are metrics-only and
# safe, but eval_records.json embeds raw chunk text from tenant documents, so
# the whole tree is ignored. Curated, sanitized samples live in
# backend/evaluation/examples/ and ARE committed.
backend/evaluation/reports/
# Built from private tenant documents (questions + ground-truth answers derived
# from real content). Regenerate locally with `manage.py build_golden_set`.
backend/evaluation/dataset/golden_set.jsonl

# ------------------------------
# Misc
# ------------------------------
*.bak
*.swp


# ------------------------------
# Frontend / Node
# ------------------------------
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

frontend/node_modules
dist
dist-ssr
*.local
.vite

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
